# ============================================
# LinkForge — Frontend Production Build
# ============================================
# This file is used when running `npm run build` in production.
# Vite replaces import.meta.env.VITE_* variables at build time.
# Without this variable set, the build will throw an error.

VITE_API_URL=https://api.linkforge.example.com